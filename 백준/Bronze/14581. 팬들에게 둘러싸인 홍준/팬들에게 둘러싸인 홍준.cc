#include <string>
#include <vector>
#include<iostream>
#include<algorithm>
#include<stack>

using namespace std;

int main()
{
    ios::sync_with_stdio(false); // 지우지 말 것.
    cin.tie(NULL); // 지우지 말 것.
    cout.tie(NULL); // 지우지 말 것.

    string s;
    cin >> s;
    cout << ":fan::fan::fan:\n:fan::" << s << "::fan:\n:fan::fan::fan:";
    
    return 0;
}
